// Just in case the user exited the game in the middle througth the modal 'x' button
$(function(){
	var div = document.getElementById('matching-game');
	if (div) {
    div.parentNode.removeChild(div);
	}
	$('#render-matching-link').after('<%= j render('players/matching_game')%>'); 
});
